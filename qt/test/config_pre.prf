include(../project/config.prf)

TEMPLATE = app
#TARGET = TestXMLValueChecker
QT = core testlib concurrent
CONFIG += precompile_header console

# we want the app bundle so it doesn't conflict with PCH output...hack!
#CONFIG -= app_bundle

INCLUDEPATH += . ../

PRECOMPILED_HEADER = ../core/Prefix.h

!*android* {
    # gcc coverage
    QMAKE_CXXFLAGS += -fprofile-arcs -ftest-coverage -O0
    macx:QMAKE_LINK += --coverage #clang
    else:LIBS += -lgcov
}

DEFINES += BUILDING_AUDACITY

SOURCES += $${TARGET}.cpp \
   ../core/QtUtil.cpp
